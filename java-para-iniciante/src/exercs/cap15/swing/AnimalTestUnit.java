/*
package exercs.cap15;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

    public class AnimalTestUnit {
        @Test
        void testDogInstance() {
            Animal dog = new Dog("Rex");
            String resultado = AnimalTest.verificaTipoAnimal(dog);
            assertTrue(resultado.contains("É um cachorro"));
            assertTrue(resultado.contains("Rex"));
        }

        @Test
        void testCatInstance() {
            Animal cat = new Cat("Felix");
            String resultado = AnimalTest.verificaTipoAnimal(cat);
            assertTrue(resultado.contains("É um gato"));
            assertTrue(resultado.contains("Felix"));
        }

        @Test
        void testGenericAnimal() {
            Animal generic = new Animal("Genérico");
            String resultado = AnimalTest.verificaTipoAnimal(generic);
            assertTrue(resultado.contains("É um animal não específico"));
        }

        @Test
        void testNullAnimal() {
            String resultado = AnimalTest.verificaTipoAnimal(null);
            assertEquals("Animal inválido", resultado);
        }
    }
}
*/
